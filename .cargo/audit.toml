# cargo-audit configuration
#
# Advisories listed here are from Pingora transitive
# dependencies that we cannot control.

[advisories]
ignore = [
    # rustls-pemfile 2.2.0: unmaintained (transitive via pingora fork)
    "RUSTSEC-2025-0134",
    # lru 0.16.4: unsound pop() panic safety (transitive via pingora fork)
    "RUSTSEC-2026-0253",
]
